package oop;

public class Book {

	class Page{
		int index;
		String[] lines;
		int wordcount;
		
		int countWord(){
			return process(lines);
		}
		
		int[] search(String word){
			search(word) in each line;
		}
	}
	
	Page[] pages;
	String author;	
	int CountWord(){
		process(pages);
	}
	 Pair<Page,int[]> search(String word){
		 
	 }
}
